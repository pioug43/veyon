# veyon package

The veyon Python package implements a Veyon WebAPI client, including browser-style
pointer/keyboard/clipboard control and native real-time teacher screen broadcasts.

See `../README.md` for the HTTP API and security requirements.
